<Window x:Class="LinkerPlayer.Windows.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LinkerPlayer"
        Width="400" Height="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Border Background="#FF2D2D30" 
            BorderBrush="#FF3F3F46" 
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Logo/Icon Area -->
            <Viewbox Grid.Row="0" Margin="50" MaxWidth="100" MaxHeight="100">
                <Image Source="/Images/app64.ico" />
            </Viewbox>
            
            <!-- App Name -->
            <TextBlock Grid.Row="1" 
                       Text="LinkerPlayer" 
                       HorizontalAlignment="Center" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       Margin="0,0,0,20"/>
            
            <!-- Loading indicator -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                <TextBlock Text="Loading..." 
                           Foreground="#FFCCCCCC" 
                           FontSize="12" 
                           VerticalAlignment="Center"/>
                <ProgressBar Name="LoadingProgress" 
                             Width="200" 
                             Height="8" 
                             Margin="10,0,0,0" 
                             IsIndeterminate="True" 
                             Style="{DynamicResource ModernProgressBar}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>