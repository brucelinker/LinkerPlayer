<UserControl
    x:Class="LinkerPlayer.UserControls.PlayerControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:LinkerPlayer.Utils"
    d:DesignHeight="80"
    d:DesignWidth="1000"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="PlayPauseButton" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="50" />
            <Setter Property="Height" Value="50" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border
                                x:Name="border"
                                Background="#EBEBEB"
                                CornerRadius="100">
                                <Image
                                    Width="20"
                                    Height="20"
                                    RenderOptions.BitmapScalingMode="HighQuality"
                                    Source="{Binding ButtonStateImagePath}" />
                            </Border>

                            <Grid.RenderTransform>
                                <ScaleTransform />
                            </Grid.RenderTransform>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="RenderTransformOrigin" Value="0.5, 0.5" />
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NoStylingButton" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Background="#212121" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition />
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="250" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="40,0,40,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>

                <Button
                    Name="PrevButton"
                    Grid.Column="0"
                    Style="{StaticResource NoStylingButton}">
                    <Image
                        Opacity="0.7"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="\Images\prev.png" />
                </Button>

                <Button
                    Name="PlayPauseButton"
                    Grid.Column="1"
                    Style="{StaticResource PlayPauseButton}" />

                <Button
                    Name="NextButton"
                    Grid.Column="2"
                    Style="{StaticResource NoStylingButton}">
                    <Image
                        Opacity="0.7"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="\Images\next.png" />
                </Button>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="65" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Name="CurrentSongName"
                        Grid.Row="0"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="/Fonts/#Montserrat"
                        FontSize="16"
                        FontWeight="DemiBold"
                        Foreground="#BDBDBD"
                        Text="Song not selected"
                        TextTrimming="CharacterEllipsis" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30*" />
                            <ColumnDefinition Width="10*" />
                            <ColumnDefinition Width="9*" />
                            <ColumnDefinition Width="17*" />
                        </Grid.ColumnDefinitions>
                        <utils:CustomSlider
                            x:Name="SeekBar"
                            Grid.ColumnSpan="4"
                            Margin="0,4,0,0"
                            VerticalAlignment="Top"
                            Panel.ZIndex="1"
                            Maximum="100"
                            ValueChanged="SeekBar_ValueChanged" />
                        <UniformGrid
                            Name="UniGrid"
                            Grid.ColumnSpan="4"
                            Margin="0,0,0,10"
                            Rows="1"
                            SizeChanged="UniGrid_SizeChanged" />
                    </Grid>
                </Grid>

                <TextBlock
                    Name="CurrentTime"
                    Grid.Column="0"
                    Padding="0,0,10,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="/Fonts/#Montserrat"
                    FontSize="15"
                    FontWeight="DemiBold"
                    Foreground="#BDBDBD">
                    0:00
                </TextBlock>
                <TextBlock
                    Name="TotalTime"
                    Grid.Column="2"
                    Padding="10,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="/Fonts/#Montserrat"
                    FontSize="15"
                    FontWeight="DemiBold"
                    Foreground="#BDBDBD">
                    0:00
                </TextBlock>
            </Grid>

            <Button
                Name="PlaybackModeButton"
                Grid.Column="2"
                Margin="10,0,0,0"
                HorizontalAlignment="Left"
                Click="PlaybackModeButton_Click"
                Style="{StaticResource NoStylingButton}">
                <Image
                    Width="40"
                    Opacity="0.7"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Source="{Binding PlaybackModeImagePath}" />
            </Button>

            <Grid Name="VolumeSlidersGrid" Grid.Column="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60*" />
                    <ColumnDefinition Width="20*" />
                    <ColumnDefinition Width="20*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="0*" />
                    <RowDefinition Height="0*" />
                    <RowDefinition Height="100*" />
                </Grid.RowDefinitions>

                <utils:CustomSlider
                    x:Name="AdditionalVolumeSlider"
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Maximum="100"
                    ValueChanged="AdditionalVolumeSlider_ValueChanged" />
                <Button
                    Name="AdditionalVolumeButton"
                    Grid.Row="0"
                    Grid.Column="1"
                    Width="20"
                    Height="20"
                    Click="AdditionalVolumeButton_Click"
                    Style="{StaticResource NoStylingButton}">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="MicrophoneVariantOff" />
                </Button>

                <utils:CustomSlider
                    x:Name="MicVolumeSlider"
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Maximum="100"
                    ValueChanged="MicVolumeSlider_ValueChanged" />
                <Button
                    Name="MicVolumeButton"
                    Grid.Row="1"
                    Grid.Column="1"
                    Width="20"
                    Height="20"
                    Click="MicVolumeButton_Click"
                    Style="{StaticResource NoStylingButton}">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="MicrophoneOff" />
                </Button>

                <utils:CustomSlider
                    x:Name="MainVolumeSlider"
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Maximum="100"
                    ValueChanged="MainVolumeSlider_ValueChanged" />
                <Button
                    Name="MainVolumeButton"
                    Grid.Row="2"
                    Grid.Column="1"
                    Width="20"
                    Height="20"
                    Click="MainVolumeButton_Click"
                    Style="{StaticResource NoStylingButton}">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="VolumeMute" />
                </Button>

                <Button
                    Grid.RowSpan="3"
                    Grid.Column="2"
                    Width="20"
                    Height="40"
                    HorizontalAlignment="Left"
                    Click="ToggleVolumeSliders"
                    Style="{StaticResource NoStylingButton}">
                    <Image
                        Name="ExpanderImage"
                        Opacity="0.7"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="/Images/expand.png" />
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
